(ns com.xwxpmnyxwp.config-reader.interface-test
  (:require [clojure.test :as test :refer :all]
            [com.xwxpmnyxwp.config-reader.interface :as config-reader]))

(deftest dummy-test
  (is (= 1 1)))
