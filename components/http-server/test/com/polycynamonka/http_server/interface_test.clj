(ns com.xwxpmnyxwp.http-server.interface-test
  (:require [clojure.test :as test :refer :all]
            [com.xwxpmnyxwp.http-server.interface :as http-server]))

(deftest dummy-test
  (is (= 1 1)))
